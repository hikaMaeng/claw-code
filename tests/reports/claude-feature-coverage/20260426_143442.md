# Test Report: claude-feature-coverage

## Timestamp

2026-04-26 14:34:42 Asia/Seoul

## Environment

- Workspace: `/mnt/f/document/New project`
- Branch: `codex/two-tier-claw-config`
- Shell: Bash
- Rust test container: `rust:1-bookworm`

## Executed Steps

1. Searched for Claude-style rule path support:
   `rg -n "\\.claude/rules|\\.claude/rule|\\.claw/rules|\\.claw/rule|join\\(\"rules\"\\)|join\\(\"rule\"\\)|rules/|rule/" rust/crates docs USAGE.md README.md`
2. Inspected prompt instruction discovery in `rust/crates/runtime/src/prompt.rs`.
3. Inspected settings hook parsing in `rust/crates/runtime/src/config.rs`.
4. Inspected runtime hook events in `rust/crates/runtime/src/hooks.rs`.
5. Inspected skill and agent discovery in `rust/crates/commands/src/lib.rs`.
6. Inspected plugin manifest compatibility validation in `rust/crates/plugins/src/lib.rs`.
7. Ran command crate skill and agent tests:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p commands skills -- --nocapture --test-threads=1`
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p commands agents -- --nocapture --test-threads=1`

## Observed Logs

- Rule path search found only the documentation note in `docs/config-home.md`.
- Prompt discovery only includes `CLAUDE.md`, `CLAUDE.local.md`, `.claw/CLAUDE.md`, and `.claw/instructions.md`.
- Runtime hook enum includes only `PreToolUse`, `PostToolUse`, and `PostToolUseFailure`.
- Settings hook parser accepts only the same three hook names.
- Skill roots are `<project>/.claw/skills`, `<project>/.claw/commands`, `$CLAW_CONFIG_HOME/skills`, and `$CLAW_CONFIG_HOME/commands`.
- Agent roots are `<project>/.claw/agents` and `$CLAW_CONFIG_HOME/agents`.
- Plugin manifest validation rejects Claude-style plugin `skills`, `agents`, `mcpServers`, command directory globs, and hook names such as `SessionStart`.
- `cargo test -p commands skills -- --nocapture --test-threads=1`: passed 6 tests.
- `cargo test -p commands agents -- --nocapture --test-threads=1`: passed 4 tests.

## Failures

None in the executed command tests.

The investigation confirmed missing feature surfaces:

- Claude-style rule directories are not implemented.
- Custom top-level slash command markdown is not implemented.
- Plugin-provided Claude Code skills, agents, MCP imports, and slash command markdown catalogs are not implemented.
- Hook support is narrower than Claude Code lifecycle hooks.

## Reproducibility

Run the listed `rg` and `docker run ... cargo test` commands from `/mnt/f/document/New project`.

## Locator Outcome

Not applicable. No browser UI was exercised.
