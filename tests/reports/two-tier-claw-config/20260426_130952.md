# Test Report: two-tier-claw-config
## Timestamp
2026-04-26 13:09:52 KST
## Environment
Docker `rust:latest`, repository mounted at `/workspace`, `CARGO_TARGET_DIR=/tmp/claw-target`, working directory `/workspace/rust`.
## Executed Steps
1. `cargo fmt --all` in Docker after installing the `rustfmt` component.
2. `cargo test -p runtime -- --nocapture`
3. `cargo test -p commands -- --nocapture`
4. Targeted tools tests:
   - `cargo test -p tools skill_resolves_project_local_skills_and_legacy_commands -- --nocapture`
   - `cargo test -p tools skill_loads_local_skill_prompt -- --nocapture`
   - `cargo test -p tools enter_and_exit_plan_mode_round_trip_existing_local_override -- --nocapture`
   - `cargo test -p tools exit_plan_mode_clears_override_when_enter_created_it_from_empty_local_state -- --nocapture`
5. Targeted CLI tests:
   - `cargo test -p rusty-claude-cli config_command_loads_defaults_from_standard_config_locations -- --nocapture`
   - `cargo test -p rusty-claude-cli resumed_config_command_loads_settings_files_end_to_end -- --nocapture`
   - `cargo test -p rusty-claude-cli resume_latest_restores_the_most_recent_managed_session -- --nocapture`
   - `cargo test -p rusty-claude-cli status_degrades_gracefully_on_malformed_mcp_config_143 -- --nocapture`
6. Inspected `F:\desktop\docker\crawcode\docker-compose.yml` and `Dockerfile`.
## Observed Logs
- Runtime: 467 unit tests and 12 integration tests passed.
- Commands: 40 unit tests passed.
- Tools targeted skill and plan-mode tests passed.
- CLI targeted config, resume, and degraded-status tests passed.
- Full `cargo test -p tools -- --nocapture` was attempted after the targeted runs; it failed on environment-dependent tests for missing PowerShell and missing `searchProvider.json` provider specs. The changed `skill_loads_local_skill_prompt` test was fixed and then passed separately.
- Docker compose now sets `CLAW_CONFIG_HOME=/home/claw/.claw`, mounts `./claw-home/.claw:/home/claw/.claw`, and no longer mounts `.codex` or `.config/claw`.
## Failures
- Full tools suite still has unrelated environment failures in this container:
  - `powershell_runs_via_stub_shell`: no `pwsh` or `powershell` in PATH.
  - `web_search_extracts_and_filters_results` and `web_search_handles_generic_links_and_invalid_base_url`: no `ddg` spec found in checked searchProvider paths.
## Reproducibility
Run the listed Docker commands from the repository root. The first run downloads crates.io dependencies and the `rustfmt` component.
## Locator Outcome
Not applicable; no browser UI was changed.
