# Test Report: settings-model-registry
## Timestamp
2026-04-26 04:57:21 KST

## Environment
Docker `rust:1-bookworm`, repository mounted at `/workspace`, `CARGO_TARGET_DIR=/tmp/claw-target`, working directory `/workspace/rust`.

## Executed Steps
1. `cargo test -p runtime -- --nocapture`
2. `cargo test -p api -- --nocapture`
3. `cargo test -p rusty-claude-cli -- --nocapture`
4. `cargo test -p api settings_provider_builds_openai_client_without_env_routing -- --nocapture`

## Observed Logs
`runtime`: 467 unit tests passed, 12 integration tests passed, 0 doc tests.

`api`: 130 unit tests passed; integration suites passed with 12 passed and 1 ignored live-network test, 6 passed, 4 passed, and 7 passed.

`rusty-claude-cli`: 179 unit tests passed; integration suites passed with 7, 3, 1, 11, and 12 tests passed.

Targeted API regression: 1 passed, confirming local OpenAI-compatible settings provider construction without `OPENAI_API_KEY` or `OPENAI_BASE_URL`.

## Failures
None in executed tests. `live_stream_smoke_test` remained ignored because it requires live Anthropic credentials and network access.

## Reproducibility
Run the commands from the plan in a clean Docker container. The first run downloads crates.io dependencies, so later runs should be faster with a persistent Cargo cache.

## Locator Outcome
Not applicable; no browser UI.
