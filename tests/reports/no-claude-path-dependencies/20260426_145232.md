# Test Report: no-claude-path-dependencies

## Timestamp

2026-04-26 14:52:32 Asia/Seoul

## Environment

- Workspace: `/mnt/f/document/New project`
- Branch: `codex/two-tier-claw-config`
- Shell: Bash
- Rust test container: `rust:1-bookworm`

## Executed Steps

1. Searched Rust crates for legacy Claude path/config names:
   `rg -n "\\.claude|CLAUDE_CONFIG_HOME|CLAUDE_CONFIG_DIR|claude-prompt-cache|\\.claude-plugin" rust/crates -g '*.rs'`
2. Moved prompt cache roots from Claude paths to Claw paths.
3. Moved plugin packaged manifest lookup from `.claude-plugin/plugin.json` to `.claw-plugin/plugin.json`.
4. Removed ignored `.claude` and `CLAUDE_CONFIG_DIR` skill fixture tests.
5. Simplified command definition source variants to Claw-only sources.
6. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p api prompt_cache -- --nocapture --test-threads=1`
7. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p commands skills -- --nocapture --test-threads=1`
8. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p commands agents -- --nocapture --test-threads=1`
9. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p plugins manifest -- --nocapture --test-threads=1`
10. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p plugins test_env_lock -- --nocapture --test-threads=1`
11. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p tools skill_ -- --nocapture --test-threads=1`
12. Ran:
   `git diff --check`

## Observed Logs

- Rust crate search returned no matches for `.claude`, `CLAUDE_CONFIG_HOME`, `CLAUDE_CONFIG_DIR`, `claude-prompt-cache`, or `.claude-plugin`.
- `cargo test -p api prompt_cache`: passed 8 unit tests and 2 integration tests.
- `cargo test -p commands skills`: passed 6 tests.
- `cargo test -p commands agents`: passed 4 tests.
- `cargo test -p plugins manifest`: passed 4 tests.
- `cargo test -p plugins test_env_lock`: passed 2 tests.
- `cargo test -p tools skill_`: passed 2 tests, 1 ignored legacy `.omc`/`.agents` test remains ignored.
- `git diff --check`: passed.
- Several Docker test runs emitted transient crates.io SSL warnings before retrying successfully.

## Failures

The first `tools` test run failed with an unmatched closing delimiter after removing ignored legacy test blocks. The leftover block tail was removed and the focused `tools` test passed on rerun.

## Reproducibility

Run the commands from the test plan in `/mnt/f/document/New project`.

## Locator Outcome

Not applicable. No browser UI is exercised.
