# Test Report: context-command
## Timestamp
2026-04-26 15:09:04 Asia/Seoul
## Environment
- Repository: `/mnt/f/document/New project`
- Runtime: Docker `rust:1-bookworm`
- Shell: bash
## Executed Steps
1. Ran `cargo test -p rusty-claude-cli context -- --nocapture --test-threads=1`.
2. Added parser and JSON/category tests for `context`.
3. Ran `/repo/rust/target/debug/claw --output-format json context` inside Docker with temp `CLAW_CONFIG_HOME`.
4. Ran Docker Compose no-cache image build from the GitHub feature branch.
5. Ran the built image with global `/workspace` settings and project `/workspace/project-ok` settings.
## Observed Logs
- `running 10 tests`
- `test tests::parses_context_as_local_diagnostic_command ... ok`
- `test tests::context_clear_action_is_rejected_as_session_mutation ... ok`
- `test tests::context_json_uses_settings_max_context_for_autocompact_buffer ... ok`
- `test result: ok. 10 passed; 0 failed; 0 ignored; 0 measured; 176 filtered out`
- CLI JSON contained `"kind": "context"`, `"max_context": 262000`, `"auto_compact_threshold": 218770`, and `"autocompact_buffer_tokens": 43230`.
- `docker compose build --no-cache --pull claw` completed `cargo build --workspace --release` inside the Docker builder stage.
- Built-image `claw --version` reported `Git SHA c9fd2a3` before the direct slash provenance fix.
- Built-image `claw --output-format json context` reported `model_source: "config"` from global settings.
- Built-image `claw --output-format json /context usage` in `/workspace/project-ok` exposed a direct slash provenance issue; direct slash parsing was corrected so settings provenance is preserved.
## Failures
None.
## Reproducibility
Run from repo root:
```bash
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli context -- --nocapture --test-threads=1
```
## Locator Outcome
Not applicable; no browser UI.
