# Test Report: model-output-budget
## Created
2026-04-26
## Environment
- Repository: `/mnt/f/document/New project`
- Docker runtime: `rust:1-bookworm`
- Deployed stack: `/mnt/f/desktop/docker/crawcode`
- Shell: bash
## Scope
Implemented settings-backed `models[].maxOutputTokens`, context-aware provider `max_tokens` calculation, output-limit stop detection, and `/context` output budget reporting.
## Commands
```bash
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p runtime max_output -- --nocapture --test-threads=1
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p runtime output_limit -- --nocapture --test-threads=1
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p runtime parses_model_provider_registry_from_settings -- --nocapture --test-threads=1
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli bounded_output_tokens -- --nocapture --test-threads=1
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli context_json_uses_settings_max_context_for_autocompact_buffer -- --nocapture --test-threads=1
git diff --check
```
## Results
- Runtime config `max_output`: passed. `rejects_zero_model_max_output_tokens` rejects `maxOutputTokens: 0`.
- Runtime conversation `output_limit`: passed. `run_turn_marks_output_limit_when_provider_reports_length_stop` preserves partial text and marks the turn output-limited.
- Runtime registry parsing: passed. `parses_model_provider_registry_from_settings` reads `maxContext` and `maxOutputTokens`.
- CLI output budget: passed. `bounded_output_tokens_reserves_context_headroom` caps output by configured max and remaining context headroom.
- CLI `/context`: passed. JSON includes `max_output_tokens`, `next_request_output_tokens`, and `output_safety_buffer_tokens`.
- `git diff --check`: passed.
## Format Check
`cargo fmt --check` could not run in `rust:1-bookworm` because the image lacks `cargo-fmt` and does not include `rustup` for installing the component. Direct Debian `rustfmt --check` also reports pre-existing unrelated formatting in files outside this change scope, so it was not used as a completion gate.
## Docker Acceptance
Pending after push: rebuild `/mnt/f/desktop/docker/crawcode` from branch `codex/two-tier-claw-config`, recreate `claw-code`, verify `claw --version`, `claw context`, project override context, and a live JSON prompt.
## Locator Contract
Not applicable; no browser UI.
