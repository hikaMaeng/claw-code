# Test Report: connected-provider-label

## Timestamp

2026-04-26 14:42:09 Asia/Seoul

## Environment

- Workspace: `/mnt/f/document/New project`
- Branch: `codex/two-tier-claw-config`
- Shell: Bash
- Rust test container: `rust:1-bookworm`

## Executed Steps

1. Reproduced the code path by inspecting `run_repl`, `format_connected_line`, `LiveCli`, `build_runtime_with_plugin_state`, and `AnthropicRuntimeClient::new`.
2. Changed the REPL connected line to read the provider kind from the constructed runtime API client.
3. Added a regression test where `qwen3.6-27b:mp` is registered to a settings provider with `"type": "openai"`.
4. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli live_cli_connected_line_uses_settings_provider_type -- --nocapture --test-threads=1`
5. Ran:
   `docker run --rm -v /mnt/f/document/New\\ project:/repo -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli format_connected_line -- --nocapture --test-threads=1`
6. Rebuilt the local Docker image from the pushed feature branch:
   `docker compose build --no-cache claw`
7. Recreated the long-running Docker service:
   `docker compose up -d --force-recreate claw`
8. Opened a TTY REPL in `/workspace` and `/workspace/model-mp`, then exited with `/quit`.

## Observed Logs

- `live_cli_connected_line_uses_settings_provider_type`: passed 1 test.
- `format_connected_line`: passed 2 tests.
- Docker build completed and produced `claw-code:local`.
- `/workspace` REPL printed `Connected: qwen3.6-35b-a3b:tr via openai`.
- `/workspace/model-mp` REPL printed `Connected: qwen3.6-27b:mp via openai`.
- The shared `tools` crate emitted an existing `dead_code` warning for `build_search_url`; it did not fail the build.
- `cargo fmt` could not run because local Bash has no `cargo`, and the `rust:1-bookworm` image has no `cargo-fmt` or `rustup` component manager.

## Failures

None in the executed tests.

## Reproducibility

Run the listed `docker run ... cargo test` commands from `/mnt/f/document/New project`. Run the listed `docker compose ...` commands from `/mnt/f/desktop/docker/crawcode`.

## Locator Outcome

Not applicable. No browser UI was exercised.
