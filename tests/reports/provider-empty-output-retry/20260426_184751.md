# Test Report: provider-empty-output-retry
## Timestamp
2026-04-26 18:47:51 Asia/Seoul
## Environment
- Repository: `/mnt/f/document/New project`
- Docker test image: `rust:1-bookworm`
- Docker Compose workspace: `/mnt/f/desktop/docker/crawcode`
- Shell: bash
## Executed Steps
1. Added runtime recovery for empty assistant streams.
2. Added CLI classification for `provider_empty_output`.
3. Ran Docker runtime tests filtered by `empty_output`.
4. Ran Docker runtime tests filtered by `provider`.
5. Ran Docker CLI classification test.
6. Ran `git diff --check`.
7. Built the Compose image with `DOCKER_BUILDKIT=1 docker compose build --no-cache --pull claw`.
8. Recreated the service with `docker compose down --remove-orphans && docker compose up -d --force-recreate claw`.
9. Verified the deployed binary with `docker exec claw-code claw --version`.
10. Verified local diagnostics with `docker exec claw-code claw context`.
11. Verified the live model path with `docker exec claw-code claw --output-format json prompt "Reply with exactly: READY"`.
## Observed Logs
- Runtime `empty_output` test: `1 passed; 0 failed`.
- Runtime `provider` tests: `8 passed; 0 failed` plus integration `1 passed; 0 failed`.
- CLI `classify_error_kind` test: `1 passed; 0 failed`.
- `git diff --check`: passed.
- `cargo fmt --check` in `rust:1-bookworm`: unavailable because the image has no `cargo-fmt` and no `rustup`.
- Compose release build completed `cargo build --workspace --release`.
- Deployed `claw --version` reported `Git SHA c37ca47`.
- `claw context` reported model `qwen3.6-35b-a3b:tr`, `max_context 262000`, and `compact@218770`.
- Live model prompt returned JSON with `"message":"\n\nREADY"`, `"model":"qwen3.6-35b-a3b:tr"`, and `iterations:1`.
## Failures
None for runtime recovery, CLI classification, Docker build, container recreate, or live model smoke test.
## Notes
- The acceptance image was built from commit `c37ca47`.
- This report is added after that acceptance run; rebuild the branch image once more if the running container must report the report-only commit SHA.
## Reproducibility
```bash
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p runtime provider -- --nocapture --test-threads=1
docker run --rm -v "/mnt/f/document/New project:/repo" -w /repo/rust rust:1-bookworm cargo test -p rusty-claude-cli classify_error_kind -- --nocapture --test-threads=1
cd /mnt/f/desktop/docker/crawcode
DOCKER_BUILDKIT=1 docker compose build --no-cache --pull claw
docker compose down --remove-orphans
docker compose up -d --force-recreate claw
docker exec claw-code claw --version
docker exec claw-code claw context
```
## Locator Outcome
Not applicable; no browser UI.
