# Test Report: docker-two-tier-config
## Timestamp
2026-04-26 14:17:15 KST
## Environment
- Source branch: `codex/two-tier-claw-config`
- Source commit built by Docker: `f5fff32`
- Compose project: `/mnt/f/desktop/docker/crawcode`
- Compose service: `claw`
- Image: `claw-code:local`
- Container: `claw-code`
## Executed Steps
1. Confirmed compose still builds from GitHub with `CLAW_REPO=https://github.com/hikaMaeng/claw-code.git` and `CLAW_REF=codex/two-tier-claw-config`.
2. Built with `docker compose build --no-cache claw`.
3. Recreated runtime with `docker compose up -d --force-recreate claw`.
4. Confirmed binary reports Git SHA `f5fff32`.
5. Ran `claw config model` and `claw status --output-format json` in `/tmp`, `/workspace/project-ok`, and `/workspace`.
6. Ran `claw --output-format json prompt ...` without `--model` in `/tmp` and `/workspace/project-ok`.
7. Ran WebSearch prompts without `--model` in `/tmp` and `/workspace/project-ok`.
8. Ran a timeout-guarded prompt without `--model` in `/workspace`, whose project provider intentionally points at a closed local endpoint.
9. Added Docker workspace project fixtures for `qwen3.6-35b-a3b:SR`, `qwen3.6-27b:mp`, and `qwen3.6-35b-a3b:SL`, then ran prompt execution in each project without `--model`.
10. Verified global state paths and absence of legacy home mounts.
11. Ran unit/parser verification in a Rust container:
    `cargo test -p rusty-claude-cli prompt_subcommand -- --nocapture --test-threads=1`.
12. Ran formatting check with Debian `rustfmt` directly:
    `rustfmt --edition 2021 --check rust/crates/rusty-claude-cli/src/main.rs`.
## Observed Logs
- Docker build clone step:
  `git clone --depth=1 --branch codex/two-tier-claw-config https://github.com/hikaMaeng/claw-code.git .`
- `claw --version`:
  `Git SHA f5fff32`
- `/tmp` config/status:
  - loaded files: `1`
  - model source: `config`
  - model: `qwen3.6-35b-a3b:tr`
  - permission mode: `danger-full-access`
- `/tmp` prompt without `--model`:
  - message: `GLOBAL_SETTINGS_MODEL_OK`
  - response model: `qwen3.6-35b-a3b:tr`
- `/tmp` WebSearch without `--model`:
  - tool use: `WebSearch`
  - result URL: `https://openai.com/`
  - response model: `qwen3.6-35b-a3b:tr`
- `/workspace/project-ok` config/status:
  - loaded files: `2`
  - model source: `config`
  - model: `qwen3.6-35b-a3b:tr`
  - permission mode: `workspace-write`
- `/workspace/project-ok` prompt without `--model`:
  - message: `PROJECT_SETTINGS_MODEL_OK`
  - response model: `qwen3.6-35b-a3b:tr`
- `/workspace/project-ok` WebSearch without `--model`:
  - tool use: `WebSearch`
  - result URL: `https://openai.com/`
  - response model: `qwen3.6-35b-a3b:tr`
- `/workspace` conflict config/status:
  - loaded files: `2`
  - model source: `config`
  - model: `docker-project-model`
  - permission mode: `read-only`
- `/workspace` conflict prompt without `--model`:
  - command timed out with `EXIT_CODE=124`
  - expected because the project provider points at `127.0.0.1:65535`
  - confirms project settings override the working global provider
- Additional project model fixtures:
  - `/workspace/model-sr`: message `MODEL_SR_OK`, response model `qwen3.6-35b-a3b:SR`
  - `/workspace/model-mp`: message `MODEL_MP_OK`, response model `qwen3.6-27b:mp`
  - `/workspace/model-sl`: message `MODEL_SL_OK`, response model `qwen3.6-35b-a3b:SL`
- Mount/config checks:
  - `CLAW_CONFIG_HOME=/home/claw/.claw`
  - `/home/claw/.claw` exists
  - `/home/claw/.codex` absent
  - `/home/claw/.config/claw` absent
  - project settings files discovered under `/workspace/**/.claw/settings.json`
- Docker health:
  - `claw-code` status: `healthy`
## Failures
- Initial pre-fix Docker runtime showed `claw prompt` without `--model` ignored settings and failed with missing Anthropic credentials.
- Fixed in `f5fff32` by resolving one-shot prompt models through the same settings fallback used by REPL, while preserving explicit `--model` precedence.
- Parallel Rust test execution can race on process-global environment variables; the targeted parser test was executed with `--test-threads=1`.
## Reproducibility
- Rebuild from GitHub branch:
  `docker compose build --no-cache claw`
- Recreate:
  `docker compose up -d --force-recreate claw`
- Global check:
  `docker compose exec -T claw sh -lc 'cd /tmp && claw --output-format json prompt "Reply with exactly GLOBAL_SETTINGS_MODEL_OK"'`
- Project check:
  `docker compose exec -T claw sh -lc 'cd /workspace/project-ok && claw --output-format json prompt "Reply with exactly PROJECT_SETTINGS_MODEL_OK"'`
- Conflict check:
  `docker compose exec -T claw sh -lc 'cd /workspace && timeout 25s claw --output-format json prompt "This should fail because project provider points at 127.0.0.1:65535"; code=$?; printf "EXIT_CODE=%s\n" "$code"'`
## Locator Outcome
Not applicable; no browser UI was changed.
