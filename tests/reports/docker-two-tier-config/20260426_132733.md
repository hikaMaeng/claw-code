# Test Report: docker-two-tier-config
## Timestamp
2026-04-26 13:27:33 KST
## Environment
- Compose project: `/mnt/f/desktop/docker/crawcode`
- Image: `claw-code:local`
- Source ref: `https://github.com/hikaMaeng/claw-code.git`, branch `codex/two-tier-claw-config`
- Service: `claw`
## Executed Steps
1. Rewrote compose to mount only:
   - `./workspace:/workspace`
   - `./claw-home/.claw:/home/claw/.claw`
2. Rewrote Dockerfile builder to clone `CLAW_REPO` at `CLAW_REF`.
3. Added project settings at `/mnt/f/desktop/docker/crawcode/workspace/.claw/settings.json`.
4. Ran `docker compose config`.
5. Ran `DOCKER_BUILDKIT=1 docker compose build claw`.
6. Ran `docker compose run --rm claw claw config model`.
7. Ran `docker compose run --rm --workdir /tmp claw claw config model`.
8. Ran `docker compose run --rm claw claw status --output-format json`.
9. Ran `docker compose run --rm --workdir /tmp claw claw status --output-format json`.
10. Ran mount/environment inspection inside the container.
11. Ran `docker compose run --rm claw claw --resume latest /status`.
12. Ran `docker compose up -d --force-recreate claw`.
13. Repeated config checks with `docker compose exec -T claw ...` against the running service container.
14. Waited for the service healthcheck and ran `docker compose ps`.
## Observed Logs
- Compose config passed `CLAW_REPO=https://github.com/hikaMaeng/claw-code.git` and `CLAW_REF=codex/two-tier-claw-config`.
- The no-cache build log showed `git clone --depth=1 --branch codex/two-tier-claw-config https://github.com/hikaMaeng/claw-code.git .`.
- Build completed and produced `claw-code:local`.
- Built binary reported Git SHA `cb0293f`.
- In `/workspace`, `claw config model` loaded 2 files:
  - `/home/claw/.claw/settings.json`
  - `/workspace/.claw/settings.json`
- In `/workspace`, merged `model` was `"docker-project-model"`.
- In `/tmp`, `claw config model` loaded 1 file:
  - `/home/claw/.claw/settings.json`
- In `/tmp`, merged `model` was the global model from `/home/claw/.claw/settings.json`.
- In `/workspace`, `claw status --output-format json` showed `model: "docker-project-model"`, `loaded_config_files: 2`, and `permission_mode: "read-only"` from project `permissions.defaultMode: "plan"`.
- In `/tmp`, `claw status --output-format json` showed the global model, `loaded_config_files: 1`, and `permission_mode: "danger-full-access"`.
- Container inspection showed:
  - `HOME=/home/claw`
  - `CLAW_CONFIG_HOME=/home/claw/.claw`
  - `/home/claw/.claw` present
  - `/workspace/.claw` present
  - `/home/claw/.codex` absent
  - `/home/claw/.config/claw` absent
- `--resume latest /status` resolved a managed session under `/home/claw/.claw/sessions/...`, confirming global-home session lookup.
- The long-running `claw-code` service was recreated from `claw-code:local`.
- Running-service checks matched the one-shot checks:
  - `/workspace` resolved `model` to `"docker-project-model"`.
  - `/tmp` resolved `model` to the global model.
  - `/home/claw/.codex` and `/home/claw/.config/claw` were absent.
- `docker compose ps` reported `claw-code` as `healthy`.
## Failures
- `claw config permissions.defaultMode` is not a supported direct config section query; status JSON was used to verify permission precedence instead.
## Reproducibility
Run the commands from this report in `/mnt/f/desktop/docker/crawcode`. The image build depends on the pushed feature branch named by `CLAW_REF`.
## Locator Outcome
Not applicable; no browser UI was changed.
